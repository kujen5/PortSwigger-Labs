[package]
name = "rust-solver"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.37", features = ["macros", "rt-multi-thread"] }

reqwest = {version = "0.12",features = ["cookies", "rustls-tls"]}

scraper = "0.20"
